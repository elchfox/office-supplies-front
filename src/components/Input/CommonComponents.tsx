import { Select } from ".";

export const CategorySelect: React.FC = (props) => {
    // const { categories = [] } = useSelector((state: RootState) => state.general)
    // console.log(categories, "categoriescategoriescategories")
    // categories.map((item) => ({ text: item.name, value: item.id || 0 }))
    return (
        <>
            <Select options={[]}  />
            </>
    );
};

